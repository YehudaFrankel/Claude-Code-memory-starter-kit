# Agreed Flows — User Journeys Locked By Decision

These flows are settled. Do not change them without explicit discussion.
They represent UX decisions, not just implementation details.

---

## [Feature Name]
```
STEP 1 → STEP 2 → STEP 3
              ↓
         [branch condition]
              ↓
         STEP 4 → done
```
**Why locked:** [reason this flow was agreed — what alternatives were rejected]

---

## How to use this file

Add a flow here when:
- A user journey has been explicitly discussed and agreed
- There was a "should we change how X works?" conversation that ended with "no, keep it"
- The flow has downstream dependencies that make changing it costly

Each flow here survived at least one "let's change how this works" conversation.
They're documented so the next conversation is shorter.
